/// MedsKai Dimensions - Consistent Spacing & Sizing
///
/// Based on website design system
///
/// Usage:
/// ```dart
/// Padding(padding: EdgeInsets.all(AppDimensions.spacingM))
/// ```
class AppDimensions {
  AppDimensions._();

  // ============================================================
  // Spacing (Padding & Margin)
  // ============================================================

  /// Extra Small - 4px
  static const double spacingXS = 4.0;

  /// Small - 8px
  static const double spacingS = 8.0;

  /// Medium - 16px
  static const double spacingM = 16.0;

  /// Large - 24px
  static const double spacingL = 24.0;

  /// Extra Large - 32px
  static const double spacingXL = 32.0;

  /// Extra Extra Large - 48px
  static const double spacingXXL = 48.0;

  // ============================================================
  // Border Radius
  // ============================================================

  /// Small radius - 8px
  static const double radiusS = 8.0;

  /// Medium radius - 10px (website buttons)
  static const double radiusM = 10.0;

  /// Large radius - 14px
  static const double radiusL = 14.0;

  /// Extra Large radius - 16px
  static const double radiusXL = 16.0;

  /// Circular radius - 20px
  static const double radiusCircular = 20.0;

  /// Full rounded - 24px
  static const double radiusFull = 24.0;

  // ============================================================
  // Button Dimensions
  // ============================================================

  /// Button height - 56px
  static const double buttonHeight = 56.0;

  /// Button height small - 44px
  static const double buttonHeightSmall = 44.0;

  /// Button horizontal padding - 52px (from website)
  static const double buttonPaddingH = 52.0;

  /// Button vertical padding - 11px (from website)
  static const double buttonPaddingV = 11.0;

  /// Button border width - 2px (from website)
  static const double buttonBorderWidth = 2.0;

  // ============================================================
  // Icon Sizes
  // ============================================================

  /// Icon extra small - 14px
  static const double iconXS = 14.0;

  /// Icon small - 18px
  static const double iconS = 18.0;

  /// Icon medium - 22px
  static const double iconM = 22.0;

  /// Icon large - 24px
  static const double iconL = 24.0;

  /// Icon extra large - 32px
  static const double iconXL = 32.0;

  // ============================================================
  // Card Dimensions
  // ============================================================

  /// Card padding - 16px
  static const double cardPadding = 16.0;

  /// Card margin - 8px
  static const double cardMargin = 8.0;

  /// Card elevation - 0 (flat design)
  static const double cardElevation = 0.0;

  // ============================================================
  // Input Field Dimensions
  // ============================================================

  /// Input height - 56px
  static const double inputHeight = 56.0;

  /// Input padding horizontal - 16px
  static const double inputPaddingH = 16.0;

  /// Input padding vertical - 14px
  static const double inputPaddingV = 14.0;

  /// Input border width - 1px
  static const double inputBorderWidth = 1.0;

  /// Input border width focused - 2px
  static const double inputBorderWidthFocused = 2.0;

  // ============================================================
  // App Bar Dimensions
  // ============================================================

  /// App bar height - 56px
  static const double appBarHeight = 56.0;

  /// App bar elevation - 0
  static const double appBarElevation = 0.0;

  // ============================================================
  // Bottom Navigation
  // ============================================================

  /// Bottom nav height - 70px
  static const double bottomNavHeight = 70.0;

  /// Bottom nav icon size - 24px
  static const double bottomNavIconSize = 24.0;

  /// Bottom nav label size - 10px
  static const double bottomNavLabelSize = 10.0;

  // ============================================================
  // Avatar Sizes
  // ============================================================

  /// Avatar small - 36px
  static const double avatarS = 36.0;

  /// Avatar medium - 48px
  static const double avatarM = 48.0;

  /// Avatar large - 60px
  static const double avatarL = 60.0;

  /// Avatar extra large - 80px
  static const double avatarXL = 80.0;

  // ============================================================
  // Course Card
  // ============================================================

  /// Course card aspect ratio - 16:9
  static const double courseCardAspectRatio = 16 / 9;

  /// Course image height ratio
  static const double courseImageHeightRatio = 220 / 375;

  // ============================================================
  // Divider
  // ============================================================

  /// Divider height - 1px
  static const double dividerHeight = 1.0;

  /// Divider thick - 2px
  static const double dividerThick = 2.0;

  // ============================================================
  // Shadow Offsets
  // ============================================================

  /// Shadow offset light
  static const double shadowOffsetY = 4.0;

  /// Shadow offset medium
  static const double shadowOffsetYMedium = 8.0;

  /// Shadow blur radius
  static const double shadowBlurRadius = 12.0;

  /// Shadow blur radius large
  static const double shadowBlurRadiusLarge = 25.0;
}
